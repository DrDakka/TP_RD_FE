enum Sex {
  M = 'm',
  F = 'f',
}

enum Activity {
  SEDENTARY = 'sedentary',
  LIGHT = 'light',
  MODERATE = 'moderate',
  ACTIVE = 'active',
  INTENSIVE = 'intensive',
}

enum Modifier {
  LOSS = 'loss',
  MAINT = 'maint',
  GAIN = 'gain',
}

export { Sex, Activity, Modifier };
